{
  "name": "back",
  "version": "1.0.0",
  "private": true,
  "description": "Auth API for the lab-localstorage lab (signup + login that returns a JWT).",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
